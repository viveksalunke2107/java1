JAVASCRIPT
- it is used to add functionaly and active ness in web page
-it is loosly typed language
-it is interperted language

@how many ways to to use operater
there are 2 ways to use operator as follows:
1.internal js : in this we have to use script tag in html file to write js code
2.external js ; in this we need to create another js code file and import it in html file by using script tag

@ways to display o/p in js 
there are 4 ways to display o/p in html as follows :
1.document.write() : in this o/p directly showen in browser page
2.console.write() : this method is used for testing purpose
3.window.alert(): in this the model page is created and display o/p in it
4.innerHTML(): it use to display o/p inside and element

@variables : it is an container used to store data in it.
-its value can be changed 
-in js we create vareables by using var,let and const.

data types in html :
1.number data type : 12345 
2.string data type : represent in ""  ,  ''
  ex "vivek",'vaishanavi'
3.array data type : represent by using [ ]
4. boolean data type ; it have only two values TRUE & FALSE

@loops in js 
1 for loop :
for (variable declaration,condition,increment/decrement)
{
    //code 
}

2.while loop :
vareable decleration
while(condetion,)
{
    //code 
    increment/decrement
}

3. do while loop :
vareable decleration
do()
{
    //code

}
while(condetion)

@ Array:
-array is an datatype
-in array user can store multiple values of same datatype
-array index always start from 0 

#there are diffrent methods of array as follows :

1.push():
in this method user can store new values in array at starting from last index
2.pop():
in this method user can remove  values in array at starting from last index
3.unshift():
in this method user can store  values in array from starting index
4.shift():
in this method user can remove  values in array from starting
5.splice(startingindex,delete count,insreting values):
in this method user can store and also remove values from array
6.tosrting () : it use to convert array data into string data
7.join (): it use to convert array data into string data it take one parameter to seperate values

@ Function:

-is is block of code which is use to perform specific task.
-function will execute(run) only when we call it.
-function can be reuse multiple time.
-in functiuon return statment must be last statment.

syntax: function functionName(){
    code...
}

*there are mainly two types function in javascript
1.inbuit (predefine) function: function which already developed and provide by javascript to us.
2.user define : function developed user to there uses.

*Rules to create user define function 
-functionName name can not be any reserved keyword name.
-function name must start with lowercase letter.
-in javascript function name can start $ and _ sign 

*Event: everything is an event in programing.
*onclick: it will work on click event.
*onMouseover: it will work whrn we over mouse on it.

*typecasting: converting one data type into another data type


1.default function:it has no parameter
2.paramitarized function:it use when user have to pass parameter
3.anonymous function :function which has no name
4.arrow function :Function which having no name &no function key word
5.optional parameterize function : in this function we can keep parameter value option ,means no need to pass parameter value
6.rest parameterize function : when we dont know how many parameter are required then we usr spread(...)operator






 

